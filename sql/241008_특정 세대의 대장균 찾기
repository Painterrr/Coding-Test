WITH FST_GEN AS (
    SELECT F.ID
    FROM ECOLI_DATA F
    WHERE PARENT_ID IS NULL
),
SCD_GEN AS (
    SELECT S.ID
    FROM ECOLI_DATA S
    JOIN FST_GEN
    ON S.PARENT_ID = FST_GEN.ID
)
    
SELECT T.ID
FROM ECOLI_DATA T
JOIN SCD_GEN
ON T.PARENT_ID = SCD_GEN.ID;
