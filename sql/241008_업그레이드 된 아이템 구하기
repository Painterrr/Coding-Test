-- 희귀도 RARE, 모든 아이템의 이전 아이템의 ID, 아이템 명, 희귀도
SELECT ITEM_ID, ITEM_NAME, RARITY
FROM ITEM_INFO
WHERE ITEM_ID IN (
    SELECT T.ITEM_ID
    FROM ITEM_INFO I, ITEM_TREE T    
    WHERE I.ITEM_ID = T.PARENT_ITEM_ID
    AND RARITY = 'RARE')
ORDER BY ITEM_ID DESC;
