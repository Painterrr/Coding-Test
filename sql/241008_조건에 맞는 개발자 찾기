-- PHTHON, C#, 개발자ID, 이메일, 성, 이름
SELECT ID, EMAIL, FIRST_NAME, LAST_NAME
FROM DEVELOPERS D
WHERE D.SKILL_CODE & (SELECT S.CODE FROM SKILLCODES S WHERE NAME = 'PYTHON')
OR D.SKILL_CODE & (SELECT S.CODE FROM SKILLCODES S WHERE NAME = 'C#')
ORDER BY ID;
