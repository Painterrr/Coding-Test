def solution(elements):
    l = len(elements)
    dp = elements
    answer = set(dp)
    elements = elements + elements
    
    for i in range(2, l):
        for j in range(l):
            dp[j] = dp[j] + elements[j + i - 1]
            answer.add(dp[j])
    
    return len(answer) + 1
