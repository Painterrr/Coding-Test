import sys
input = sys.stdin.readline

n = int(input())
people = list(map(int, input().split()))

people.sort()

minute = 0
sum = 0
for p in people:
    minute = minute + p
    sum += minute
    
print(sum)
